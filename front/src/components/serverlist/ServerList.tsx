import ServerIcon from "./ServerIcon";

interface Props {
}

const ServerList = (props: Props) => {
    return (
        <div className="h-full scrollbar-hide overflow-y-scroll bg-gray-900 w-16 flex-shrink-0">
            <div className="flex flex-col space-y-2 items-center p-2">
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
                <ServerIcon />
            </div>
        </div>
    )
};

export default ServerList;
